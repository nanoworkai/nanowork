# Python
__pycache__
*.py[cod]
*$py.class
.venv
venv
.python-version
*.egg-info
.eggs
dist
build
apps/api/static/
apps/api/.pytest_cache/
apps/api/.mypy_cache/
apps/api/.ruff_cache/

# Node
node_modules
*.local

# Lock files (keep package-lock.json only)
bun.lock
yarn.lock
pnpm-lock.yaml

# IDE
.idea
.vscode/*
!.vscode/extensions.json
*.swp
.DS_Store

# Env
.env
.env.local
.env.*.local
.env-backup/
*.env.backup

# Build artifacts (historical root build or mis-targeted builds)
/dist
apps/web/dist/

# Claude Code worktrees
.claude/worktrees/

# Deployment configs (keep only active platform)
# Uncomment to ignore unused deployment configs:
# vercel.json
# wrangler.toml
