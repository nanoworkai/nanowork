{
  "name": "nanowork-mvp",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev:web\" \"bun run dev:worker\"",
    "dev:web": "cd apps/web && bun run dev",
    "dev:worker": "cd apps/worker && npx wrangler dev",
    "build": "cd apps/web && bun run build",
    "start": "npm run start -w web",
    "deploy": "bun run build && bun run deploy:worker && bun run deploy:web",
    "deploy:worker": "cd apps/worker && npm run validate && npx wrangler deploy",
    "deploy:web": "cd apps/web && npx wrangler pages deploy dist",
    "typecheck": "cd apps/web && bun run typecheck && cd ../worker && npm run typecheck",
    "lint": "npm run lint -w web && cd apps/worker && npm run lint",
    "validate": "bun run typecheck && npm run lint",
    "prepare": "simple-git-hooks",
    "generate:og": "bun scripts/generate-og-image.js"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "lint-staged": "^15.2.11",
    "playwright": "^1.60.0",
    "simple-git-hooks": "^2.12.1"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "apps/worker/src/**/*.ts": [
      "cd apps/worker && npm run typecheck",
      "cd apps/worker && npm run lint"
    ],
    "apps/web/src/**/*.{ts,tsx}": [
      "cd apps/web && tsc --noEmit"
    ]
  }
}
