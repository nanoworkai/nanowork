# Optional absolute API origin when the SPA and API live on different hosts.
# Leave unset for dev (Vite proxies /api to FastAPI at http://127.0.0.1:8000).
# Amplify: set https://YOUR_API_HOSTNAME in Environment variables → VITE_API_BASE_URL
# VITE_API_BASE_URL=https://api.example.com
